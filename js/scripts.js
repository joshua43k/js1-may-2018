function calculateVisits(returnUser) {
  let visitCount = 0;
  visitCount++;
  function logCount() {
    console.log(visitCount);
  }
  return logCount();
  if(returnUser) return visitCount

}

calculateVisits(true);


import React from 'react';

export default App extends Component {
  render();
  return (
    <Component />
  )
}

function Component(one, two three) {
  this.one = one;
  // .....
}
new Component(render(), template)














//
